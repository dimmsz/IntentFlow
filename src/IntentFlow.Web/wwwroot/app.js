const text=document.querySelector("#text"),result=document.querySelector("#result"),shopping=document.querySelector("#shopping"),status=document.querySelector("#speechStatus");let items=JSON.parse(localStorage.getItem("intentflow.shopping")||"[]");
function render(){shopping.innerHTML="";items.forEach(x=>{const li=document.createElement("li");li.textContent=x;shopping.appendChild(li)});localStorage.setItem("intentflow.shopping",JSON.stringify(items))}
async function parse(){const value=text.value.trim();if(!value)return;const r=await fetch("/api/parse",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({text:value})});const d=await r.json();result.textContent=JSON.stringify(d,null,2);if(d.intent==="AddShoppingItem"){(d.items||[]).forEach(x=>{if(!items.includes(x.name))items.push(x.name)});render()}}
document.querySelector("#parse").onclick=parse;
const SR=window.SpeechRecognition||window.webkitSpeechRecognition;
if(SR){const rec=new SR();rec.lang="ja-JP";rec.interimResults=false;rec.continuous=false;document.querySelector("#mic").onclick=()=>{status.textContent="聞いています…";rec.start()};rec.onresult=e=>{text.value=e.results[0][0].transcript;status.textContent="音声を取得しました";parse()};rec.onerror=e=>status.textContent="音声入力エラー: "+e.error;rec.onend=()=>{if(status.textContent==="聞いています…")status.textContent=""}}else{document.querySelector("#mic").disabled=true;status.textContent="このブラウザは音声入力に対応していません"}render();